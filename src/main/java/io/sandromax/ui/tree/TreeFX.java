package io.sandromax.ui.tree;

import javafx.scene.control.TreeItem;
import javafx.scene.control.TreeView;

public class TreeFX {
    public static void newProject(TreeView<String> treeView, String name) {
//        TreeItem<String> treeItem = new TreeItem<>(name);
//        treeView.setRoot(treeItem);
    }

    public static void renameProject(TreeView<String> treeView, String name) {
//        TreeItem<String> treeItem = new TreeItem<>(name);
//        treeView.setRoot(treeItem);
    }

}
